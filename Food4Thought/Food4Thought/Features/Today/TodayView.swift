import SwiftUI

/// Home / Today — handoff 1d, 1e, 10a.
struct TodayView: View {
    var body: some View {
        ComingSoonScreen(
            title: "Today",
            note: "The rings and meal list land next. Log a meal with the + button — that's what fills them."
        )
    }
}
