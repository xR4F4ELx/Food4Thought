import SwiftUI

/// Foods — the user's own library: custom foods, favourites, saved presets.
struct FoodsView: View {
    var body: some View {
        ComingSoonScreen(
            title: "Foods",
            note: "Your saved foods and favourites. Star something while logging and it shows up here."
        )
    }
}
